/**
 * Pipeline E2E Test IDs — re-exports from shared/testids.ts
 *
 * The canonical testid helpers live in shared/testids.ts so both
 * frontend components and E2E tests use the same source of truth.
 */
export {
  pipeline,
  dealCard,
  dealDetail,
  callScheduling,
  deck,
  pipelineSettings,
  tabs,
} from "../../shared/testids";
